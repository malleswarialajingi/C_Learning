#include<stdio.h>
int main ()
{
	//base of the triangle = base;
	//height of the triangle = height;
	//area of a triangle = area;
	int base = 10;
	int height = 5;
	int area =(base * height)/2;
	printf("%d", area);
}
